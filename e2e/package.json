{
  "name": "atrium-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Atrium end-to-end tests (Playwright, Page Object Model)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  }
}
